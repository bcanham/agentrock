<h1><%= title "Agent Rock's Blog" %></h1>

<% @admin_articles.each do |admin_article| %>
  <tr>
    <td><%= link_to 'Show', admin_article %></td>
    <td><%= link_to 'Edit', edit_admin_article_path(admin_article) if admin_signed_in? %></td>
    <td><%= link_to 'Destroy', admin_article, :confirm => 'Are you sure?', :method => :delete  if admin_signed_in? %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to 'New Article', new_admin_article_path  if admin_signed_in? %>
