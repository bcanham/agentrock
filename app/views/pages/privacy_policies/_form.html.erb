<%= form_for(@pages_privacy_policy) do |f| %>
  <% if @pages_privacy_policy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pages_privacy_policy.errors.count, "error") %> prohibited this pages_privacy_policy from being saved:</h2>

      <ul>
      <% @pages_privacy_policy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
