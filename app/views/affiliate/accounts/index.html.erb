<h1>Listing affiliate_accounts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @affiliate_accounts.each do |affiliate_account| %>
  <tr>
    <td><%= link_to 'Show', affiliate_account %></td>
    <td><%= link_to 'Edit', edit_affiliate_account_path(affiliate_account) %></td>
    <td><%= link_to 'Destroy', affiliate_account, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Account', new_affiliate_account_path %>
